# TODO

## Bash Tool

- [x] Add `description` parameter for command intent
- [x] Add `run_in_background` parameter for long-running commands
- [x] Improve description: guide AI to use dedicated tools instead of bash for file ops
- [ ] Add output truncation to prevent excessive token usage
- [ ] Change timeout unit to milliseconds with max cap (align with Claude Code)
