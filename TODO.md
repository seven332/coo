# TODO

## Agent Tool

- [x] `description` parameter — short 3-5 word task description for logging/display
- [ ] `run_in_background` — async execution, don't block parent agent
- [ ] `isolation: worktree` — git worktree isolation for safe parallel file modifications
- [ ] `subagent_type` — agent type system (Explore, Plan, custom agents)
- [ ] `name` + SendMessage — named agents, resumable conversations
- [ ] Fork optimization — inherit parent context for prompt cache reuse
